<main>
  <h1>It works!</h1>
  <p>This is @@NAME@@, a collage project.</p>
  <p>
    Edit <code>templates/pages/home.html</code> and refresh — template changes
    reload automatically while <code>COLLAGE_DEV=1</code> is set (which
    <code>collage dev</code> sets for you). Changing this project's Go code
    still requires stopping and re-running <code>collage dev</code>.
  </p>
</main>
