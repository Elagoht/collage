{
  "_comment": [
    "Each key is a plugin's Name(). The site loads this file itself and hands the",
    "framework a map, so nothing here is imposed by collage — an application whose",
    "settings live in YAML or the environment passes the same map from there.",
    "",
    "The entries below are inert: this example registers no plugins, so a key",
    "naming one would be a startup error. Add the plugin in site.go and its",
    "section here together."
  ]
}
